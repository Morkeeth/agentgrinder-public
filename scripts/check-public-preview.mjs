import assert from 'node:assert/strict';
import {writeFile} from 'node:fs/promises';
import {readFileSync} from 'node:fs';
import {readPublic,html,card,privateCard} from '../server/public-run.mjs';
import {BRAND} from '../server/brand.mjs';
import {ImageResponse} from '@vercel/og';
const id='28d5d0b7-eda2-4d94-a83c-580d2e3b75b2';let fetches=0;
assert.equal(await readPublic('bad',()=>{fetches++}),null);assert.equal(fetches,0);
const sample=JSON.parse(readFileSync(new URL('../samples/sample_run.json',import.meta.url),'utf8'));
const fixture={...sample,id,title:'Story layout fixture',caption:'Labelled example data from samples/sample_run.json.',output_url:'https://github.com/Morkeeth/agentgrinder-public/blob/main/samples/sample_run.json',started_at:sample.started,prompts:sample.turns_typed,visibility:'public',profiles:{github_handle:'fixture-builder'}};
assert.deepEqual(await readPublic(id,async(url,options)=>{assert.equal(new URL(url).searchParams.get('visibility'),'eq.public');assert(!new URL(url).searchParams.get('select').includes('note'));assert(new URL(url).searchParams.get('select').includes('github_handle,handle,display_name'));assert(new URL(url).searchParams.get('select').includes('wall_time_s'));assert(new URL(url).searchParams.get('select').includes('shell_calls'));assert(new URL(url).searchParams.get('select').includes('route'));assert.equal(options.cache,'no-store');assert.equal(options.headers['Accept-Profile'],'strava');return{ok:true,json:async()=>[fixture]}}),fixture);
assert.equal(await readPublic(id,async()=>({ok:true,json:async()=>[]})),null);
assert(!html({...fixture,title:'<script>"bad"</script>'}).includes('<script>'));
const chosen={...fixture,profiles:{handle:'chosen-builder',github_handle:null,display_name:'Chosen Builder'}};
assert(JSON.stringify(card(chosen)).includes('@chosen-builder'),'chosen-only identity appears on the share card');
assert(JSON.stringify(card(fixture)).includes('@fixture-builder'),'legacy identity fallback survives');
assert(!JSON.stringify(card({...chosen,visibility:'close_friends'})).includes('@chosen-builder'),'private audiences omit identity');
const renamed=JSON.stringify(card({...chosen,profiles:{...chosen.profiles,github_handle:'old-alias'}}));
assert(renamed.includes('@chosen-builder')&&!renamed.includes('@old-alias'),'chosen identity takes precedence');
const meta=html(chosen);for(const tag of ['property="og:image"','property="og:title"','property="og:description"','name="twitter:card" content="summary_large_image"'])assert(meta.includes(tag),tag+' missing');
const render=async(tree,path)=>{const image=new ImageResponse(tree,{width:1200,height:630});const bytes=Buffer.from(await image.arrayBuffer());assert.equal(bytes.subarray(1,4).toString(),'PNG');assert.equal(bytes.readUInt32BE(16),1200);assert.equal(bytes.readUInt32BE(20),630);await writeFile(path,bytes);return bytes};
await render(card(chosen),'/tmp/strive-public-og.png');
assert(JSON.stringify(card(chosen)).includes(BRAND),'the public card carries the brand');
const neutral=JSON.stringify(privateCard());assert(neutral.includes('This run is private on '+BRAND));assert(!neutral.includes('fixture-builder'));
await render(privateCard(),'/tmp/strive-private-og.png');
if(process.argv.includes('--live')) {const live=await readPublic(id);assert(live?.id===id);assert(html(live).includes('og:image'));}
console.log('Preview: sample and neutral PNGs rendered at 1200x630; metadata, public-only query, identity privacy, and private/missing neutral contract passed');
