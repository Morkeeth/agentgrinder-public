# Grok Bot template kit

Status: source kit, not an installed or published template. Uses the reviewed native Grok adapter. A real second-bot installation and hosted post remain required.

Add the instructions in `post-agent-run/SKILL.md` as the bot's post-run skill through the current Grok Bot skill interface. Keep this repository available on its computer so the bundled helper can run. Do not claim a one-click import format: this kit has no verified marketplace manifest.

Bot identity: help the owner show real agent work to friends and other builders. Capture and preview locally. Preserve the white card and blue trace. Let the owner choose the exact post and audience. No automated engagement.

Configuration: independent product origin; owner-selected export path; no credentials or transcripts in the shared template. Do not add scheduled posting routines by default.

Try the helper against the labelled sample first. Then install on a second bot, select its real export, verify measured counts against that export, open the private preview, and let the owner approve a post. Have a second person open/respond to it and verify that withdrawing it removes access. Record installed, tested and published independently.

Before sharing the template, inspect the actual exported package for account tokens, conversation history, files and personal configuration. Publish the template only after the owner approves that exact package and destination.
